# Telegram-бот для портфеля облигаций и акций

Бот на **aiogram 3.x**:
1. При первом `/start` регистрирует пользователя (SQLite).
2. Пользователь добавляет бумаги в портфель: тип (облигация/акция), тикер MOEX, количество.
3. Каждые `CHECK_INTERVAL_HOURS` часов (по умолчанию 6) бот сам присылает по каждой бумаге:
   - текущую цену и дневное изменение (MOEX ISS API),
   - 1–2 свежих новости по эмитенту,
   - информационный сигнал «держать / докупить / зафиксировать прибыль» на основе изменения цены за неделю.
4. Есть кнопка «📰 Сводка сейчас» — получить сводку вне расписания.

⚠️ Сигналы — это простая эвристика по цене, **не индивидуальная финансовая рекомендация**. В каждом сообщении есть дисклеймер.

## Структура проекта

```
telegram_bond_bot/
├── main.py                 # точка входа
├── config.py                # чтение .env
├── database/db.py           # SQLite: users, portfolio
├── handlers/                # /start, добавление/удаление бумаг, сводка
├── keyboards/                # reply/inline клавиатуры
├── services/
│   ├── moex.py               # цены и доходности с MOEX ISS (публичный API, без ключа)
│   ├── news.py               # новости: NewsAPI (по ключу) или Google News RSS (бесплатно)
│   ├── analysis.py           # эвристика рекомендаций
│   └── scheduler.py          # APScheduler: рассылка раз в N часов
├── states/forms.py           # FSM для добавления бумаги
├── tests/                    # offline-тесты БД и эвристики
├── requirements.txt
├── .env.example
└── Dockerfile
```

## Установка и запуск

1. Получите токен бота у [@BotFather](https://t.me/BotFather) (`/newbot`).
2. Скопируйте `.env.example` в `.env` и укажите `BOT_TOKEN`:
   ```
   cp .env.example .env
   ```
   `NEWS_API_KEY` необязателен — без него новости будут браться через бесплатный Google News RSS. Ключ NewsAPI (бесплатный тариф) можно получить на https://newsapi.org — даёт более релевантную выдачу.
3. Установите зависимости:
   ```
   pip install -r requirements.txt
   ```
4. Запустите бота:
   ```
   python main.py
   ```
   или через Docker (обязательно так, с монтированием тома `data`, иначе база обнулится при следующем обновлении — см. раздел ниже):
   ```
   docker build -t bond-bot .
   docker run -d --name bond-bot --env-file .env -v $(pwd)/data:/app/data bond-bot
   ```

## Как не потерять базу данных при обновлении бота

По умолчанию `DB_PATH=data/bot.db` — файл БД лежит в подпапке `data/`, отдельно от кода. Это сделано специально, чтобы обновление не задевало данные. Правила зависят от способа запуска.

**Если вы уже пользовались более ранней версией бота, где одна и та же бумага могла появиться в портфеле несколькими строками** — при первом запуске обновлённого кода `Database.init()` автоматически найдёт такие дубли, сложит их количество в одну запись и создаст уникальный индекс, чтобы дубли больше не появлялись. Ничего вручную чистить не нужно, просто обновите код и перезапустите бота как обычно (см. ниже, как не потерять сам файл `data/bot.db` при обновлении).

### Запуск через Docker

Обновление обычно выглядит как пересборка образа и пересоздание контейнера — а любые файлы, записанные *внутри* контейнера (в его writable-слое), при этом теряются безвозвратно. Чтобы данные выжили, каталог `data/` должен быть смонтирован как **volume с хоста**, а не находиться только внутри контейнера:

```
docker build -t bond-bot .
docker stop bond-bot && docker rm bond-bot
docker run -d --name bond-bot --env-file .env -v $(pwd)/data:/app/data bond-bot
```

Проверка: путь в `-v хост:контейнер` (`/app/data`) должен совпадать с директорией из `DB_PATH` внутри контейнера (`data/bot.db` относительно `/app`, то есть `/app/data/bot.db`). Если поменяете `DB_PATH` — поменяйте и вторую часть `-v` соответственно. После `docker rm` файл `./data/bot.db` на хосте остаётся на месте, и новый контейнер, запущенный с тем же `-v`, увидит прежние данные.

Docker Compose (эквивалентно, но удобнее для повторных обновлений):
```yaml
services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
```
Тогда обновление — это `docker compose build && docker compose up -d`, и том `./data` не пересоздаётся.

### Запуск напрямую на сервере (без Docker)

Если обновляете код через `git pull` в той же рабочей директории — `data/bot.db` не входит в git (см. `.gitignore`) и `git pull` его не тронет, всё сохранится само.

Осторожнее, если деплой устроен так, что каждый раз делается **свежий checkout в новую директорию** (например, CI/CD с `releases/<timestamp>` и симлинком `current`, как в Capistrano-подобных схемах) — тогда `data/` при каждом деплое будет создаваться заново, пустой. В этом случае вынесите `DB_PATH` за пределы репозитория, например:
```
DB_PATH=/var/lib/telegram-bond-bot/bot.db
```
и убедитесь, что эта директория создана один раз и доступна на запись пользователю, от которого работает бот (код сам создаст файл и поддиректории при старте, если у процесса есть права на запись).

### В любом случае — делайте бэкапы

SQLite — это один файл, бэкап тривиален:
```
cp data/bot.db backups/bot-$(date +%F).db
```
Можно добавить это в cron перед каждым обновлением.


## Как пользоваться

- `/start` — регистрация.
- «➕ Добавить бумагу» → выбрать тип → ввести тикер MOEX (например `SBER`, `GAZP` — акции; `SU26238RMFS4`, `RU000A1035V6` — облигации) → указать количество. Если такая бумага (тот же тикер и тип) уже есть в портфеле — новое количество прибавится к существующему, а не создаст отдельную строку.
- «📊 Мой портфель» — список бумаг.
- «🗑 Удалить бумагу» — удалить одну из бумаг.
- «📰 Сводка сейчас» — получить сводку немедленно, не дожидаясь расписания.
- Автоматическая рассылка — каждые `CHECK_INTERVAL_HOURS` часов всем, у кого портфель не пуст.

## Что реально проверено в этой среде разработки

Песочница, в которой собирался проект, не имеет доступа к `api.telegram.org` и `iss.moex.com` (сеть ограничена служебными доменами вроде PyPI/GitHub), поэтому "живой" прогон бота с реальным Telegram и биржевыми данными здесь невозможен. Проверено то, что можно проверить без внешней сети:

- ✅ Установка всех зависимостей из `requirements.txt`.
- ✅ Синтаксическая корректность всех модулей (`py_compile`).
- ✅ Сборка приложения целиком: создание `Bot`, `Dispatcher`, регистрация всех роутеров, инициализация планировщика — без ошибок импорта/связывания.
- ✅ `tests/test_database.py` — регистрация пользователя (включая защиту от дублей), добавление/чтение/удаление бумаг, группировка портфелей по пользователям — на реальном SQLite через `aiosqlite`.
- ✅ `tests/test_analysis.py` — вся матрица эвристики рекомендаций (рост/падение/нейтрально/нет данных, акции и облигации).

Что стоит проверить самостоятельно при первом запуске с реальным токеном и интернетом:
- корректность конкретных тикеров MOEX (структура ответа ISS стабильна, но для облигаций поле `YIELD` заполняется не всегда — тогда бот покажет сигнал без доходности);
- лимиты NewsAPI бесплатного тарифа (100 запросов/день) — при большом портфеле и коротком интервале рассылки может не хватить, тогда автоматически используется Google News RSS.

## Возможные доработки

- Хранить историю цен в БД, чтобы не дёргать MOEX историю каждый раз.
- Добавить пороговые значения (% изменения) как настройку пользователя.
- Добавить уведомления о приближении даты погашения/оферты облигации (поле `MATDATE`/`OFFERDATE` в ISS).
- Многопользовательский rate-limit при рассылке (сейчас сообщения отправляются последовательно).
